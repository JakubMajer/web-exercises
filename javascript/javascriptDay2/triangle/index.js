'use strict'

const angle1= Number(prompt('enter angle1'));
const angle2= Number(prompt('enter angle2'));
const angle3= Number(prompt('enter angle3'));


if (angle1 + angle2 + angle3 === 180) {
    document.write('yes that is tirangle');

} else {
    document.write('no that is not a triangle');
}