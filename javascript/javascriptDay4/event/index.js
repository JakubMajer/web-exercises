'use strict'

const handleClick = () => {
    console.log('string printer after clicking');
}

const btnELm = document.querySelector('#btn');
btnElm.addEventListener('click', (e) => {
    console.log('message');
});


document.addEventListener('keydown', () => {
    console.log('down');
});

const handleKeyUp = (e) => {
    if
}